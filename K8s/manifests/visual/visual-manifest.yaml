apiVersion: v1
kind: ServiceAccount
metadata:
  name: k8-visualizer-sa
  namespace: default
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: k8-visualizer-role
  namespace: default
rules:
- apiGroups: [""]
  resources: ["pods", "services", "nodes"]
  verbs: ["get", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: k8-visualizer-binding
  namespace: default
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: k8-visualizer-role
subjects:
- kind: ServiceAccount
  name: k8-visualizer-sa
  namespace: default
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: visual-deployment
  labels:
    app: visualizer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: visualizer
  template:
    metadata:
      labels:
        app: visualizer
    spec:
      containers:
      - name: visualizer-contain
        image: shashank9928/visual-img:10
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: visual-service
spec:
  selector:
    app: visualizer
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
