# Ensure the main Prometheus server is exposed via ClusterIP
prometheus:
  service:
    type: ClusterIP
  ingress:
    enabled: false
  prometheusSpec:
    externalUrl: http://afeb3ae60d49341e0a1f2b857d2f260a-1241394927.us-west-2.elb.amazonaws.com/prometheus
    routePrefix: /prometheus

# Ensure Alertmanager is exposed via ClusterIP
alertmanager:
  service:
    type: ClusterIP
  ingress:
    enabled: false
  alertmanagerSpec:
    externalUrl: http://afeb3ae60d49341e0a1f2b857d2f260a-1241394927.us-west-2.elb.amazonaws.com/alertmanager
    routePrefix: /alertmanager

# Ensure Grafana is exposed via ClusterIP
grafana:
  grafana.ini:
    server:
      root_url: "%(protocol)s://%(domain)s/grafana"
      serve_from_sub_path: true
  service:
    type: ClusterIP
  ingress:
    enabled: false
  resources:
    requests:
      memory: "512Mi"
      cpu: "250m"
    limits:
      memory: "1Gi"
      cpu: "500m"

# ServiceMonitors are fine as they are, they define how Prometheus scrapes
serviceMonitors:
  - name: backend-metrics
    namespace: default # Ensure this matches the namespace of your backend app
    selector:
      matchLabels:
        app: backend
    endpoints:
      - port: http # This should match the name of the port in your backend-service
        path: /metrics

        interval: 15s


